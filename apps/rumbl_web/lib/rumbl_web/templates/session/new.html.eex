<h1>Login</h1>
<%= form_for @conn, Routes.session_path(@conn, :create), [as: :session], fn f -> %>
<!-- Plug.conn structs are useful when you’re creating forms that aren’t backed by a changeset, such as a login or search form.  -->
  <div>
    <%= text_input f, :username, placeholder: "Username" %>
  </div>
  <div>
    <%= password_input f, :password, placeholder: "Password" %>
  </div>
  <%= submit "Log in" %>
<% end %>
